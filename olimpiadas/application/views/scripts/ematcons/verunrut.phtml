<? if ($this->layout()->detallerut) {

    $datos = $this->layout()->detallerut;
    //print_r($datos);
    echo "<table><tr>";
    foreach($datos[0] as $key => $value)
    {
        echo "<td>$key</td>";
    }
    echo "</tr><tr></tr>";
    foreach($datos[0] as $key => $value)
    {
        echo "<td>$value</td>";
    }
    echo "</tr></table>";
}
?>